apiVersion: v1
kind: Service
metadata:
  name: {{ include "mrp-service.fullname" . }}
  labels:
    {{- include "mrp-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "mrp-service.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
